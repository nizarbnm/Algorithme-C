
#include<stdlib.h>
#include<stdio.h>

struct name
{
char * realname;
char * Familyname;
float height;
float weight;
};

int main(int argc, const char * argv[])
{
struct Person.realname = "Nizar";
struct Person.Familyname = " Ben Mbarek";
struct person.float height = 6.25;
struct Person.float weight = 235;

printf ("%s is the hero name %. He is %2f ft tall and weight %1f lbs\n", person.realname,
          person.familyname, person.height,person.weight);
		  return 0;
		  
}
